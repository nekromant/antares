Для STM32 у нас есть несколько специальных целей для make 

'make list-interrupts' 
      Выведет полный список имен векторов прерываний, который есть в выбранном 
      в конфигурации MCU. Чтобы не приходилось вечно лазать по исходникам. 

'make stm32probe'
      запустит stm32flash, пощупает подключенный MCU, и запишет в конфиг
      правильные значения размера RAM и FLASH. Оно так же задетектит density 
      и линейку для STM32 контроллеров, которые поддерживает ANTARES.

Happy hacking! 
